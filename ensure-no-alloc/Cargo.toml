[package]
name = "ensure-no-alloc"
version = "0.1.0"
publish = false
edition = "2018"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
bytesize = { path = "..", default-features = false }
